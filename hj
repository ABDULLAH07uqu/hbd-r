<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HBD RENAD ✨</title>
    <style>
        body { margin: 0; background: #000; color: #ff4d6d; display: flex; align-items: center; justify-content: center; min-height: 100vh; font-family: sans-serif; overflow: hidden; }
        .card { text-align: center; background: #0a0a0a; padding: 40px; border-radius: 30px; border: 2px solid #ff4d6d; box-shadow: 0 0 30px #ff4d6d; width: 85%; max-width: 400px; z-index: 10; position: relative; }
        #name { font-size: 4rem; font-weight: 900; color: #fff; text-shadow: 0 0 20px #ff4d6d; margin: 20px 0; height: 80px; letter-spacing: 5px; }
        #msg { display: none; color: #ffccd5; font-size: 1.3rem; line-height: 1.6; margin-top: 20px; font-family: sans-serif; }
        button { background: #ff4d6d; color: #fff; border: none; padding: 15px 40px; border-radius: 50px; font-size: 1.4rem; cursor: pointer; font-weight: bold; margin-top: 10px; }
        .cake { font-size: 60px; margin-bottom: 10px; }
        .heart { position: fixed; bottom: -10%; font-size: 25px; animation: fly 4s linear forwards; pointer-events: none; }
        @keyframes fly { to { bottom: 110%; transform: translateX(100px); opacity: 0; } }
    </style>
</head>
<body>
    <div class="card">
        <div class="cake">🎂</div>
        <div id="name"></div>
        <div id="msg">
            كل عام وأنتِ لقلبي مصدر النور، ❤️<br> 
            كل عام وأنتِ لروحي الفرح.. <br>
            يا أغلى من سكن قلبي، ميلادك هو عيدي. ✨
        </div>
        <button id="btn">اضغطي هنا يا ريناد 🎵</button>
    </div>

    <script>
        const btn = document.getElementById('btn'), nameDiv = document.getElementById('name'), msgDiv = document.getElementById('msg');
        
        btn.onclick = () => {
            btn.style.display = 'none';
            // نغمة عيد ميلاد بسيطة ومضمونة
            const ac = new(window.AudioContext||window.webkitAudioContext)();
            const play = (f, t) => {
                const o = ac.createOscillator(), g = ac.createGain();
                o.frequency.value = f; g.gain.setValueAtTime(0.1, t);
                g.gain.exponentialRampToValueAtTime(0.0001, t + 0.5);
                o.connect(g); g.connect(ac.destination);
                o.start(t); o.stop(t + 0.5);
            };
            [261, 261, 293, 261, 349, 329].forEach((f, i) => play(f, ac.currentTime + i * 0.5));

            // تأثير كتابة الاسم والرسالة
            let n = "RENAD", i = 0;
            const t = setInterval(() => {
                if(i < n.length) nameDiv.innerHTML += n[i++];
                else { clearInterval(t); msgDiv.style.display = 'block'; }
            }, 500);

            // تأثير القلوب المتطايرة
            setInterval(() => {
                const h = document.createElement('div');
                h.className = 'heart'; h.innerHTML = '❤️';
                h.style.left = Math.random() * 100 + 'vw';
                document.body.appendChild(h);
                setTimeout(() => h.remove(), 4000);
            }, 300);
        };
    </script>
</body>
</html>
